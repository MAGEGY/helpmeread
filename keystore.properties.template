# Copy this file to keystore.properties and fill in your release keystore details.
# Generate a keystore with:
#   keytool -genkey -v -keystore helpmeread-release.keystore -alias helpmeread -keyalg RSA -keysize 2048 -validity 10000
# keystore.properties is gitignored — never commit it.

storeFile=helpmeread-release.keystore
storePassword=YOUR_STORE_PASSWORD
keyAlias=helpmeread
keyPassword=YOUR_KEY_PASSWORD
